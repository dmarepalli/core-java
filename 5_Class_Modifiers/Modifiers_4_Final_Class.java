/**
 * 
 * If a class is declared as final then we can not create 
 * child class to that class.
 * 
 * If a method is declared as final then we can not override that
 * method in child classes.
 * 
 * Regular classes can contain final methods.
 * In a final class, by default all the methods are final.
 *
 */
final class Modifiers_4_Final_Class {

	final void m1(){
		
	}
}
