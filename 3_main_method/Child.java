
public class Child extends Parent{
// Inheritance is applicable for static methods including main() also
// Hence if child class does not contain main() then
// parent's main() will be executed 	
}
